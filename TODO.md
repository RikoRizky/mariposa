# TODO - Responsif `index.html`

## Step 1
- Update `index.html` CSS agar layout responsif: ganti `100vh` ke `100dvh` (dengan fallback) dan buat ukuran `.flow .img` mengikuti viewport. ✅




## Step 2
- Update preloader `transform-origin` jadi adaptif (via CSS variable), diset oleh JS berdasarkan ukuran viewport. ✅


## Step 3
- Refactor JS animasi flow supaya menghitung ulang ukuran/posisi saat `resize`/orientation change (debounced), tanpa merusak urutan animasi. ✅



## Step 4
- Jalankan pengecekan cepat: buka `index.html` di desktop lalu resize; cek juga simulasi mobile (portrait/landscape). ✅



